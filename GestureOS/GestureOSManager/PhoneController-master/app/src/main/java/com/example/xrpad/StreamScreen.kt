package com.example.xrpad

import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier

// 현재 MainActivity에서 XRPadApp을 직접 쓰고 있어서 StreamScreen은 사실상 미사용.
// 하지만 너 프로젝트에 파일이 존재하면 "컴파일은" 되어야 하므로, 최소 형태로 정상화해둠.
@Composable
fun StreamScreen(modifier: Modifier = Modifier) {
    // unused
}
