# 비밀 키 (절대 커밋 금지)
.env
credentials.json

# 파이썬 캐시
__pycache__/
*.pyc

# 사용자 데이터 (거래 내역, 예산 등 개인정보) 및 내보내기 결과물
data/

# PyInstaller/Inno Setup 빌드 결과물 (용량 크고 다시 만들 수 있음 - kurima.spec/kurima_setup.iss로
# 재빌드. 실제 설치 파일은 git에 안 올리고 GitHub Releases로 배포)
build/
dist/
installer_output/
