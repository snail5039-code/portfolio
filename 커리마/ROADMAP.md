# 커리마 — 현재 구조·로드맵 문서

`PLAN.md`는 가계부 전용 CLI였던 시절의 원본 설계 문서라 그대로 남겨두고, 이 문서는 지금의
"커리마"(PC 전반을 다루는 개인비서)가 **왜 이런 구조가 됐는지**와 **앞으로 뭐가 남았는지**를
정리한다. 실제 사용법은 [README.md](README.md), 세션 인수인계는 [HANDOFF.md](HANDOFF.md),
자잘한 할 일은 [TODO.md](TODO.md), 실행 흐름은 [ARCHITECTURE.md](ARCHITECTURE.md) 참고.

## 0. 시작 배경 — 왜 만들었고, 왜 확장했는지

처음엔 "Gemini Function Calling으로 자연어 가계부 에이전트 만들기"라는 **과제**로 시작했다
(`Personal_financial_management.txt`가 그 원본 기획서, `PLAN.md`가 그 시절 설계 문서).
과제 자체는 거래 등록/조회/수정/삭제 + 예산 관리 + 카테고리 관리 정도의 범위로 완료됐다
(`기본_CLI/`, `PLAN.md`에 남아있는 tool 10~11개짜리 버전).

과제를 완료한 뒤, "이왕 만든 김에 실제로 매일 쓸 수 있는 내 개인비서로 확장하면 어떨까"라는
생각으로 범위를 넓히기 시작했다. 가계부 하나만 다루던 좁은 구조로는 할일·메모·PC 제어·파일
관리·음성·구글 연동처럼 서로 다른 도메인이 계속 늘어나는 걸 감당하기 어려워서, 폴더 이름부터
`가계부_도우미` → `나만의_종합_에이전트`로 바꾸고 "도메인별 `tools_*.py` + 중앙 레지스트리
(`tools.py`)" 구조로 다시 짰다(자세한 구조는 2절). 그 뒤로 "진짜 자비스처럼" 상시 대기하며
알림을 보내고, 음성으로 부르면 답하고, 컴퓨터가 꺼져 있어도 재설치해서 쓸 수 있는 설치
파일까지 만드는 방향으로 계속 확장해왔다(로드맵 이력은 4절).

즉 **`PLAN.md`(과제 결과물, 가계부 전용) → 이 문서(그 이후 확장된 결과, 종합 개인비서)**의
관계다.

## 1. 개요

- **목적**: Gemini Function Calling으로 자연어 명령을 도구 호출로 변환해 PC 전반(가계부·할일·
  메모·PC 제어·파일·음성·구글 연동 등)을 다루는 개인비서
- **실행 형태 두 가지**:
  1. `app.py` — 터미널에서 직접 대화하는 대화형 CLI (Rich로 화면을 그림)
  2. `tray_app.py` — 컴퓨터가 켜져 있는 동안 백그라운드에 상주하며 알림·음성 웨이크워드를 처리하는
     별도 프로세스 (`app.py`와 독립적으로 실행되지만 도구 파이프라인은 그대로 재사용함)
- **사용 모델**: `gemini-3.6-flash` (Gemini Interactions API)
- **배포 형태**: 소스 실행(`python app.py`) 또는 PyInstaller+Inno Setup으로 만든 설치 파일
  (`KurimaSetup.exe`, GitHub Releases에 올라감) — 자세한 내용은 8절 참고

## 2. 파일 구조

`기본_CLI/`는 원본 가계부 전용 CLI의 스냅샷으로 **개발 중단 상태**다(더 이상 갱신 안 함).
실제 개발은 전부 `나만의_종합_에이전트/`에서 진행한다.

`나만의_종합_에이전트/`는 도메인별로 `tools/tools_*.py` 파일을 나누고, `tools/__init__.py`가
이걸 전부 모아 Gemini에 넘길 스키마 목록(`TOOLS`)과 이름→함수 매핑(`FUNCTION_MAP`)을 만드는
구조다. 새 기능을 추가할 때 손대는 파일이 항상 같은 패턴(`tools/tools_*.py`에 함수+스키마 추가
→ `tools/__init__.py`의 `REGISTRY`에 등록 → 위험한 동작이면 `CONFIRM_MESSAGES`에도 등록)이라,
파일이 늘어나도 구조가 헷갈리지 않는다.

구글 API 호출은 `google_api/`로 따로 뺐다 — 여기엔 Gemini용 스키마가 없고 순수하게 구글
서버와 통신하는 부품만 있다. `tools/tools_calendar.py`처럼 Gemini에 노출할 도구가 이걸 포장해서
쓰고, Gemini를 거치지 않는 `tray_app.py`의 백그라운드 루프는 `google_api/`를 직접 호출한다.
루트에는 진입점(`app.py`, `agent_cli.py`, `tray_app.py`, `voice_assistant.py`)과 패키징 파일만
남겼다. 전체 파일 지도는 [README.md의 "구조" 절](README.md#구조)에 있다.

## 3. 도구 현황 (총 53개)

| 도메인 | 개수 | 비고 |
|---|---|---|
| 가계부(거래·예산·카테고리) | 10 | 원본 기획 |
| 할일 | 5 | 구글 할일(Tasks) 연동 |
| 메모 | 3 | |
| 생활 계산기 | 4 | 더치페이/D-day/만나이/근무일수 |
| 날씨·환율 | 2 | |
| 뉴스 브리핑 | 1 | |
| PC 상태 | 1 | |
| 클립보드 | 2 | |
| 화면 인식 | 1 | 스크린샷 + Gemini vision |
| PC 제어 | 5 | 앱 실행/종료, 볼륨, 화면 잠금 — 강제종료는 확인 필요 |
| 파일 관리 | 7 | 이동/이름변경/삭제는 확인 필요 |
| Gmail | 2 | 읽기 전용 |
| 구글 캘린더 | 2 | 조회 + 일정 추가(추가는 확인 필요) |
| 자동 브리핑 설정 | 5 | 실제 알림은 트레이 앱이 보냄 |
| 로컬 문서 검색(RAG) | 2 | Gemini 임베딩 비용 발생하는 유일한 기능 |
| 되돌리기 | 1 | 도메인 무관 공용 |

새 도구를 등록할 때 되돌리기 어려운 동작(삭제·강제종료·일정 추가 등)이면 `tools/__init__.py`의
`CONFIRM_MESSAGES`에 등록해서, 터미널이면 y/n으로, 음성 웨이크워드 중이면 삐 소리 + "네"로
확인을 받게 한다(자세한 흐름은 [ARCHITECTURE.md](ARCHITECTURE.md) 참고).

## 4. 로드맵 이력

원래 "가계부 전용 CLI"였던 걸 "진짜 자비스처럼" 만들자는 목표로 단계적으로 확장했다.

| 단계 | 내용 | 상태 |
|---|---|---|
| Phase 0 | 가계부 전용 CLI (거래·예산·카테고리·되돌리기) | 완료 |
| Phase 1 | 할일·메모·생활계산기·날씨/환율·뉴스·PC 상태·클립보드·화면 인식·PC 제어·파일 관리로 확장 | 완료 |
| Phase 2 | 클로드/코덱스 모드 (설치된 CLI를 커리마 안에서 호출) | 완료 |
| Phase 3 | Gmail·구글 캘린더 연동(조회) | 완료 |
| Phase 4-1 | 상시 실행 트레이 앱 (자동 브리핑 + 다운로드 알림) | 완료 |
| Phase 4-2 | 음성 웨이크워드 ("커리마"라고 부르면 응답) | 완료 |
| Phase 4-3 | 선제적 제안 (캘린더 일정 임박 시 먼저 알림) | 완료 |
| Phase 5 | 로컬 문서 검색(RAG) | 완료 |
| Phase 6 | 실사용 버그 수정 (트레이 앱 무음/COM 초기화 오류, 폴더 검색 등) | 완료 |
| Phase 7 | 구글 캘린더 일정 추가(쓰기) + 음성 확인 흐름 | 완료 |
| Phase 8 | 설치 파일(.exe) 패키징 (PyInstaller + Inno Setup) | 완료 |

Computer Use(마우스/키보드 자동 조작)는 비밀번호·결제 화면을 실수로 건드릴 위험 때문에
로드맵에서 제외하기로 결정했다(대신 위험한 동작은 전부 확인을 받는 현재 방식 유지).

## 5. 안전장치 원칙

- **되돌리기 어려운 동작은 항상 실행 전에 확인받는다** — 터미널 y/n, 음성이면 "네"로 답하는
  방식. 애매하거나 답이 없으면 안전하게 취소 쪽으로 판단한다.
- **삭제는 완전 삭제가 아니라 휴지통으로** 보낸다(파일 삭제).
- **Gmail은 읽기 전용**, 구글 캘린더는 조회+추가까지만(수정/삭제 아직 없음) — 실수로 메일을
  지우거나 엉뚱한 일정을 고치는 사고를 원천 차단.
- **Computer Use(화면 자동 클릭)는 하지 않는다** — 비밀번호/결제 화면 위험 때문에 로드맵에서
  의도적으로 제외.

## 6. 배포 (설치 파일) 만들 때 주의할 점

`kurima.spec`(PyInstaller)로 `Kurima.exe`(대화형 CLI)와 `KurimaTray.exe`(트레이 앱)를 만들고,
`kurima_setup.iss`(Inno Setup)로 관리자 권한이 필요 없는 사용자 폴더(`%LOCALAPPDATA%\Programs\Kurima`)
설치 파일을 만든다. 개인 재설치 목적이라 정식 배포(코드사이닝, 자동 업데이트 등)까지는 안 하고,
GitHub Releases에 실행 파일만 올려둔다. 다시 빌드할 때 특히 주의할 점:

- PyInstaller로 얼린 실행 파일 안에서는 `__file__` 기반 상대 경로가 exe 위치를 못 가리켜서,
  `sys.executable` 기준으로 분기해야 한다(THIS_DIR을 쓰는 모든 파일에 적용됨).
- 콘솔이 없는 트레이 앱(`console=False`)에서는 `sys.stdin`/`sys.stdout`이 `None`이라, 이걸
  그냥 쓰는 코드는 얼린 상태에서만 터진다.
- Rich 콘솔에서 마스코트 트루컬러 배경이 제대로 나오려면 `Console(color_system="truecolor",
  force_terminal=True, legacy_windows=False)`가 다 필요하다 — 하나라도 빠지면 얼린 실행 파일
  환경에서 색이 안 나올 수 있다.
- `pyttsx3`(TTS)가 쓰는 COM은 스레드별로 초기화 상태가 안 맞으면 "CoInitialize가 호출되지
  않았습니다" 오류가 나므로, 매 호출마다 `pythoncom.CoInitialize()`/`CoUninitialize()`로 감싼다.

## 7. 다음에 고려할 방향

- 새 exe를 빌드할 때마다 6절과 같은 클래스의 문제가 재발할 수 있으니 그 체크리스트를
  우선 확인할 것.
- 모바일에서 가계부·할일·메모·날씨·환율·뉴스·Gmail·캘린더만 텔레그램 봇으로 쓰는 안을
  검토했으나, 배포 계획이 없어 보류(PC 제어 등 나머지 기능은 "이 컴퓨터"를 조작하는 거라
  폰에서는 의미가 없거나 원격 제어가 따로 필요함).
- `google_api/google_tasks.py`가 `google_api/google_auth.py`와 별개로 `credentials.json` 경로를
  중복 계산하고 있음 — 패키징 자체엔 문제없지만 언젠가 `google_auth.get_service()`로 통합할
  여지 있음.

**커밋 규칙**: 한국어 + `feat:`/`fix:`/`refactor:`/`docs:` 같은 conventional 접두사 유지.
