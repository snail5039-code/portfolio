# 나중에 할 일

로드맵 전체 이력과 "왜"는 [ROADMAP.md](ROADMAP.md), 완료된 것 요약은 [HANDOFF.md](HANDOFF.md) 참고.
여기는 아직 안 끝난 것만 남긴다.

## 직접 테스트해봐야 할 것

- **"내 자료 색인해줘"로 실제 색인 돌려보기** — 진짜 사용자 Documents/Desktop 폴더를
  색인하면 시간이 오래 걸리고 Gemini 임베딩 API 비용도 들어서, 아직 실제 폴더로는
  검증 안 됨(테스트 문서로만 로직 확인 완료). 파일이 몇 개나 있는지에 따라 첫 색인이
  좀 걸릴 수 있음.
- **설치 마법사의 "Windows 시작 시 자동 실행" 체크박스** — 코드(`kurima_setup.iss`)만
  짜뒀고, 실제로 컴퓨터를 재부팅해서 트레이 앱이 자동으로 뜨는지까지는 아직 확인 안 됨.

## 정리하면 좋은 것 (동작엔 문제없는 기술 부채)

- `google_tasks.py`가 `google_auth.py`와 별개로 `credentials.json` 경로를 자기 방식대로
  다시 계산하고 있음 — `google_auth.get_service()`로 통합하면 중복이 없어짐.

## 보류 중인 것

- **모바일(텔레그램 봇)로 가계부·할일·메모·날씨·환율·뉴스·Gmail·캘린더만 옮기기** —
  검토는 했지만 "배포 안 할 거라 굳이 필요 없다"는 결론으로 보류. 나중에 외부에 공유할
  일이 생기면 다시 꺼내볼 것.

## 완료된 로드맵 (참고용)

가계부 전용 CLI로 시작해서 지금의 종합 개인비서 + 설치 파일까지, 전체 단계별 이력은
[ROADMAP.md의 "로드맵 이력" 절](ROADMAP.md)에 표로 정리돼 있다. 여기서 다시 나열하지 않는다.
