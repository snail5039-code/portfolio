# 커밋 메시지 컨벤션

이 저장소에 커밋할 때는 아래 규칙을 따릅니다.

## 기본 형식

```
type: 제목

본문 (선택, 권장)
```

- **제목**: `타입: 무엇을 했는지` 한 줄로 간결하게. 끝에 마침표 찍지 않기.
- **줄바꿈**: 제목과 본문 사이는 반드시 한 줄 띄우기.
- **본문**: 무엇을(what) 보다 왜(why) 바꿨는지 위주로 작성. 여러 항목이면 `-` 목록 사용.

### 예시

```
feat: 캘린더 회고봇 기획문서 추가

주간 캘린더 일정을 기반으로 회고 리포트를 생성하는
n8n 워크플로우 기획 내용을 정리해서 추가.
```

```
fix: 사주챗봇 운세 API 에러 처리 누락 수정

- 필수 입력값(name, birthDate) 누락 시 400 응답 처리 추가
- Gemini 응답이 비어 있을 때 기본 안내 문구 반환
```

## 타입 (type) 목록

| 타입 | 의미 | 사용 시점 |
| --- | --- | --- |
| `feat` | 기능 추가 | 새로운 기능/화면/API를 추가했을 때 |
| `fix` | 버그 수정 | 잘못 동작하던 부분을 고쳤을 때 |
| `docs` | 문서 | README, 기획문서 등 문서만 추가·수정했을 때 |
| `comm` | 주석 | 코드 동작 변경 없이 주석만 추가·수정했을 때 |
| `style` | 스타일 | 포맷팅, 세미콜론, 들여쓰기 등 동작에 영향 없는 변경 |
| `refactor` | 리팩토링 | 동작은 그대로 두고 코드 구조/가독성을 개선했을 때 |
| `test` | 테스트 | 테스트 코드를 추가·수정했을 때 |
| `chore` | 기타 | 빌드 설정, 의존성, `.gitignore` 등 그 외 잡다한 변경 |

## 권장 사항

- 하나의 커밋에는 하나의 작업만 담기 (기능 추가 + 버그 수정을 한 커밋에 섞지 않기)
- 제목만으로 부족하면 반드시 본문에 이유를 남기기 (나중에 `git log`만 보고도 왜 바꿨는지 알 수 있게)
- 프로젝트 폴더명이 커밋 범위를 더 명확히 해준다면 `feat(사주챗봇): ...`처럼 스코프를 붙여도 좋음

## 커밋 전 체크리스트

- [ ] **모든 폴더가 최신 파일로 되어 있는지 확인.** 특히 `GestureOS`, `J-E-Trace`, `WorkLog_project`, `lastcall`, `my-little-restaurant`, `commute-battle`처럼 원래 자기만의 GitHub 저장소가 따로 있던 프로젝트는 이 portfolio 저장소에 소스가 통째로 복사되어 있는 것이라, 원본 저장소에서 작업을 계속하면 여기와 내용이 달라질 수 있음. 커밋 전에 원본 저장소 최신 커밋을 이 폴더로 다시 복사해서 동기화했는지 확인
- [ ] 새 폴더/파일을 옮기거나 복사할 때 예전 버전과 중복(이중 폴더)으로 남아있지 않은지 확인
- [ ] `.env`, API 키, 비밀번호 등 민감정보가 하드코딩된 채로 커밋되지 않는지 확인 (`git add -A --dry-run` 또는 `git status`로 스테이징될 파일 목록 확인)
- [ ] 새 프로젝트를 추가했다면 그 폴더의 README와 루트 [README.md](./README.md)의 프로젝트 목록도 같이 갱신
