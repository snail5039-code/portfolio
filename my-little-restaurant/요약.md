# 요약 — 나만의 작은 맛집

## 배포 링크

https://my-little-restaurant.vercel.app

## 개선한 점

- **AI 맛집 추천 기능 추가**: Gemini API 연동. 기분·날씨·상황을 자유롭게 적으면 저장된 맛집 중에서 AI가 2~3곳을 골라 이유와 함께 추천.
  - **보안 강화**: 프롬프트에 "코드/DB 구조/어드민 정보 언급 금지" 명시
- **AI 챗봇 위젯 추가**: 화면에 돌아다니는 캐릭터를 클릭하면 열리는 채팅창. 저장된 맛집 목록과 정부 인증 모범음식점 데이터를 함께 참고해서, 실제 등록된 가게 이름과 지역을 근거로 답변하도록 구현 (없는 가게를 지어내지 않게 프롬프트로 제한).
  - **보안 강화**: 프롬프트에 "코드/DB 구조/어드민 정보/설정 언급 금지" 명시, 일상대화만 가능하도록 제한
- **필터 기능 확장**
  - 맛집 리스트에 카테고리 필터에 이어 **지역(구/군/시) 필터** 추가
  - 지도 화면에 **전체 / 내 맛집만 / 모범업소만** 보기 필터 추가 (내 맛집 마커와 모범업소 마커가 겹쳐서 안 보이는 문제 해결)
- **카드 리스트 페이지네이션** 추가 (맛집이 100곳 넘어가면서 한 페이지에 다 보여주던 걸 12개씩 나눔)
- **디자인 리뉴얼**: 공공기관 참고 사이트 느낌으로 전체 색상 톤을 따뜻한 앰버/골드 계열로 교체
- **커뮤니티 게시판 글 수정 기능** 추가 (기존엔 삭제만 가능했음) — 카테고리/제목/내용/사진까지 전부 수정 가능
- **대전 지역 실제 맛집 더미데이터 70곳** 추가 (본인 실거주지 반영, 실제 상호명 기반)

## 막혔던 점

- **AI 연동 실패 — 모델명 문제**: 챗봇이 계속 500 에러를 냈던 원인이 `gemini-1.5-flash` 모델이 이미 단종(deprecated)된 것이었음. Gemini API를 직접 curl로 호출해서 에러 메시지를 확인하고, `ListModels`로 실제 사용 가능한 모델 목록을 조회한 뒤 최신 모델로 교체하고서야 해결됨. 화면에 뜨는 뭉뚱그린 에러만 봐서는 원인을 알 수 없었고, API를 직접 두들겨봐야 정확한 원인이 나왔음.
- **챗봇 캐릭터 클릭 실패**: "챗봇이 안 열린다"는 버그가 꽤 오래 재현이 안 돼서 애먹음. 알고 보니 드래그 기능을 구현하며 넣은 `setPointerCapture`가 크로미움에서 클릭(click) 이벤트 합성 자체를 막아버리는 게 원인이었음. 자동화 도구로 좌표를 직접 dispatch하는 테스트는 이 문제를 못 잡아냈고 (히트테스트를 건너뛰기 때문), 실제 화면 좌표를 클릭하는 방식으로 재현하고서야 잡을 수 있었음.

## 새롭게 배운 점

- **`setPointerCapture`가 클릭을 막을 수 있다는 것**: 포인터 캡처는 스펙상 click 이벤트에 영향을 주면 안 되지만, 실제로는 드래그 시작 지점을 캡처해두면 이후 click 합성이 억제되는 경우가 있었음. 드래그와 클릭이 같이 필요한 UI는 캡처보다 document 레벨 리스너가 더 안전함.
 * 쉽게 말해 한가지가 레벨이 더 높아서 클릭이 제한되는 현상이 있다는 것, 그래서 사용하는 도구를 변경함
- **자동화 클릭 테스트의 함정**: `dispatchEvent`로 직접 이벤트를 강제 발생시키면 실제 브라우저의 히트테스트(어떤 엘리먼트가 진짜 그 좌표에서 클릭을 받는지)를 건너뛰기 때문에, "테스트는 통과하는데 실제로는 안 되는" 상황이 생길 수 있음. 실제 좌표를 클릭하는 방식으로 검증해야 신뢰할 수 있음.
 * 테스트 데이터 검증과 실제 검증이 다르다는 점을 알았다.
- **무료 지오코딩(Nominatim/OpenStreetMap)**: 카카오맵 REST API 키가 없어도, OpenStreetMap의 Nominatim으로 실제 주소를 좌표로 변환할 수 있었음 (초당 1회 제한은 있지만 무료).
 * 무료로 더미데이터의 주소를 좌표로 변환할때 사용하는 것
- **Next.js의 fetch 캐싱 동작을 직접 확인하는 법**: 데이터가 최신인지 의심될 때 추측하지 않고, 실제 응답 헤더(`cache-control`, `x-vercel-cache`)를 확인해서 캐싱 여부를 검증하는 습관이 중요했음.
 * 코딩 시 기능이 정상동작 하지 않으면 우선 데이터가 최신인지 아닌지 확인
- **Supabase RLS 정책은 `pg_policies`로 바로 조회 가능**: "왜 데이터가 안 보이지?"를 추측하는 대신 실제 정책(SELECT/INSERT/UPDATE 권한)을 SQL로 바로 확인하면 원인 파악이 훨씬 빠름.
 * AI가 추측을 많이 하는 경우가 있는 데 이럴 경우 직접 조회로 명령
- **AI 프롬프트 보안 강화의 중요성**: LLM에 system prompt를 통해 강하게 제한하지 않으면, 사용자가 유도 질문(jailbreak)을 할 때 코드/DB 구조/어드민 정보가 노출될 수 있음. 프롬프트에 "DO NOT show code, never mention admin functions, security rules strictly enforce" 같은 명시적 제한을 넣어야 함.
 * 실제로 스크린샷에서 챗봇이 파이썬 코드와 어드민 권한 정보를 보여줌 → 프롬프트로 완전히 차단
- **보완 관련** : 보안 기능을 걸어놓지 않으면 간단하게 관리자 권한, API 키 노출, 관련 없는 것들 등을 유출 당할 수 있다는 것을 알았다. 모든 개발 이후에는 보안 조치를 해놓아야 한다는 것을 알았다. 
