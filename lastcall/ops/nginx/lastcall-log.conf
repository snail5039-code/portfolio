# Do not write query strings to the LastCall access log because emergency
# searches can include precise latitude and longitude in URL parameters.
log_format lastcall_noargs '$remote_addr - $remote_user [$time_local] '
                           '"$request_method $uri $server_protocol" $status $body_bytes_sent '
                           '"$http_referer" "$http_user_agent"';
